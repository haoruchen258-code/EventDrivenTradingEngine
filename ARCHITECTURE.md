# 系统架构

## 概述

EventDrivenTradingEngine 采用事件驱动架构（Event-Driven Architecture），以模拟实时金融交易系统。系统以市场数据（ticks）作为事件源，通过一系列组件处理这些事件，实现自动化交易决策和执行。

## 核心组件

### 1. 数据馈送 (Data Feed)
- **文件**: `data_feed.h`, `data_feed.cpp`
- **功能**: 加载 CSV 格式的市场数据
- **数据结构**: `Tick` (包含日期和价格)
- **接口**: `loadCSV()` 函数

### 2. 策略 (Strategy)
- **文件**: `strategy.h`, `strategy.cpp`
- **功能**: 基于价格历史计算交易信号
- **算法**: 移动平均线交叉策略 (短期/长期均线)
- **信号类型**: BUY, SELL, HOLD
- **接口**: `computeSignal()` 函数

### 3. 投资组合 (Portfolio)
- **文件**: `portfolio.h`, `portfolio.cpp`
- **功能**: 管理现金、持仓和交易历史
- **数据结构**:
  - `Trade`: 单笔交易记录
  - `Portfolio`: 整体投资组合状态
- **接口**: `createPortfolio()`, `executeBuy()`, `executeSell()`, `getNetValue()`

### 4. 风险管理 (Risk Manager)
- **文件**: `risk.h`, `risk.cpp`
- **功能**: 监控投资组合风险，实施熔断机制
- **指标**: 峰值股权、当前回撤、最大回撤
- **熔断规则**:
  - 回撤超过阈值时禁止新买入
  - 强制平仓以控制损失
- **接口**: `createRiskManager()`, `updateRisk()`

### 5. 引擎 (Engine)
- **文件**: `engine.h`, `engine.cpp`
- **功能**: 协调所有组件，驱动事件循环
- **流程**:
  1. 初始化投资组合和风险管理器
  2. 逐个处理市场数据 ticks
  3. 计算交易信号
  4. 执行交易（受风险控制）
  5. 更新状态并记录日志
- **接口**: `runEngine()` 函数

## 数据流

```
市场数据 (CSV) → 数据馈送 → 引擎
                          ↓
策略计算 ← 价格历史 ← 引擎
                          ↓
交易执行 ← 风险检查 ← 引擎
                          ↓
投资组合更新 → 日志输出
```

## 事件驱动机制

- **事件源**: 市场数据 ticks（每个 tick 代表一个价格更新事件）
- **事件循环**: 引擎中的主循环，持续处理事件
- **响应机制**: 每个事件触发信号计算、交易决策和状态更新
- **异步处理**: 支持 Ctrl+C 中断，模拟实时系统控制

## 扩展性

系统设计为模块化，便于扩展：

- 新策略：实现新的 `computeSignal` 变体
- 新风险规则：修改 `RiskManager` 逻辑
- 新数据源：扩展 `DataFeed` 接口
- 新资产类型：增强 `Portfolio` 结构

## 性能考虑

- 使用标准 C++ 容器和算法
- 内存效率：向量存储价格历史
- I/O 优化：批量写入 CSV 日志
- 实时模拟：循环重放数据以测试持续运行