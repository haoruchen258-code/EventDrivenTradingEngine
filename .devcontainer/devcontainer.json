{
  "image": "mcr.microsoft.com/devcontainers/cpp:latest",
  // postCreateCommand 在 Codespace 首次创建后自动执行一次
  // 第一步：apt-get update 刷新软件包列表（不刷新则找不到 python3-pip）
  // 第二步：安装 python3-pip
  // 第三步：用 pip 安装绘图依赖
  "postCreateCommand": "sudo apt-get update -qq && sudo apt-get install -y -qq python3-pip && python3 -m pip install pandas matplotlib --quiet --break-system-packages",
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-vscode.cpptools",
        "ms-vscode.cmake-tools"
      ]
    }
  }
}